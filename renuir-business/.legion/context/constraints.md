# Constraints

- Use `pnpm` only for dependency and script management.
- Keep changes inside `renuir-business/` for this project.
- Do not depend on or modify user-global OpenCode config in `~/.config/opencode`.
- Keep `.legion` as the source of project context and execution policy.
- Browser QA is the only active QA scope for this repo right now.
